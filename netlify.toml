[build]
  command = "npm run build"
  publish = "dist/public"
  base = "" # Add your base directory here, e.g., "frontend" or "app"

# Redirecionamento para suportar SPA (React Router, wouter, etc.)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200